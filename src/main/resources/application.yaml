spring:
  application:
    name: tinylink

  datasource:
    url: jdbc:mysql://localhost:3306/tinylinkdb
    username: admin
    password: secret

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

debug: true